name: Tabletop Incident Injects

on:
  schedule:
    - cron: "*/6 * * * *"
  workflow_dispatch:
    inputs:
      reset:
        description: "Start a new random inject? (true = new, false = continue)"
        required: true
        type: choice
        options: ["false","true"]
        default: "true"

permissions:
  contents: write
  issues: write

jobs:
  injects:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main || true

      - name: Select or continue inject file
        id: select
        run: |
          set -euo pipefail
          RESET="${{ github.event.inputs.reset }}"
          STATE_FILE="state.json"

          shopt -s nullglob
          files=(inject-*.txt)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No inject-*.txt files found." >&2
            exit 1
          fi

          if [ "$RESET" = "true" ] || [ ! -f "$STATE_FILE" ]; then
            RANDOM_FILE=${files[RANDOM % ${#files[@]}]}
            INDEX=0
            echo "{\"current_inject\":\"${RANDOM_FILE}\",\"index\":${INDEX}}" > "$STATE_FILE"
            echo "Picked new inject: $RANDOM_FILE"
          else
            FILE=$(jq -r .current_inject "$STATE_FILE")
            INDEX=$(jq -r .index "$STATE_FILE")
            TOTAL=$(wc -l < "$FILE" | tr -d ' ')
            if [ "$INDEX" -ge "$TOTAL" ]; then
              RANDOM_FILE=${files[RANDOM % ${#files[@]}]}
              INDEX=0
              echo "{\"current_inject\":\"${RANDOM_FILE}\",\"index\":${INDEX}}" > "$STATE_FILE"
              echo "Previous inject completed, picked new: $RANDOM_FILE"
            fi
          fi

          FILE=$(jq -r .current_inject "$STATE_FILE")
          INDEX=$(jq -r .index "$STATE_FILE")

          echo "file=$FILE" >> $GITHUB_OUTPUT
          echo "index=$INDEX" >> $GITHUB_OUTPUT

      - name: Post inject issue
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          FILE="${{ steps.select.outputs.file }}"
          INDEX="${{ steps.select.outputs.index }}"
          LINE=$(sed -n "$((INDEX+1))p" "$FILE")

          USERS=("A.Arkin" "B.Pitt" "C.Theron" "D.Washington" "E.Wood")
          HOSTS=("BDP-SRV01" "TMX-LAP03" "ADS-MAIL02")
          IPS=("185.36.88.54" "92.143.21.100" "45.77.222.13")

          USER=${USERS[$RANDOM % ${#USERS[@]}]}
          HOST=${HOSTS[$RANDOM % ${#HOSTS[@]}]}
          IP=${IPS[$RANDOM % ${#IPS[@]}]}
          TIMESTAMP=$(date -u +'%Y-%m-%d %H:%M:%S UTC')

          TITLE="🚨 [${FILE}] Line $((INDEX+1)) - $TIMESTAMP"
          BODY=$(printf "**Inject %s from %s**\n\n%s\n\n**User:** %s\n**Host:** %s\n**IP:** %s\n" \
            "$((INDEX+1))" "$FILE" "$LINE" "$USER" "$HOST" "$IP")

          curl -s -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${REPO}/issues" \
            -d "$(jq -nc --arg t "$TITLE" --arg b "$BODY" '{title:$t, body:$b, labels:["Tabletop","Alert"]}')"

      - name: Update index and commit state
        run: |
          FILE="${{ steps.select.outputs.file }}"
          INDEX="${{ steps.select.outputs.index }}"
          NEXT=$((INDEX+1))
          jq --argjson i "$NEXT" '.index=$i' state.json > tmp.json && mv tmp.json state.json

          git add state.json
          git commit -m "Advance index to $NEXT for $FILE" || true
          git push origin main || true

      - name: Check if inject finished and post summary
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          FILE=$(jq -r .current_inject state.json)
          INDEX=$(jq -r .index state.json)
          TOTAL=$(wc -l < "$FILE" | tr -d ' ')
          if [ "$INDEX" -lt "$TOTAL" ]; then
            echo "Not finished ($INDEX / $TOTAL)"
            exit 0
          fi

          SUMMARY="### Completed Inject File: $FILE\n\n**All injects processed:**\n"
          while IFS= read -r line; do
            SUMMARY="${SUMMARY}\n- ${line}"
          done < "$FILE"

          TITLE="Summary - $FILE completed"
          curl -s -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${REPO}/issues" \
            -d "$(jq -nc --arg t "$TITLE" --arg b "$SUMMARY" '{title:$t, body:$b, labels:["Tabletop","Summary"]}')"

          rm -f state.json
          git rm -f state.json || true
          git commit -m "Reset state after finishing $FILE" || true
          git push origin main || true